import { Text, View } from "react-native";

export default function VoiceScreen() {
  return (
    <View
      style={{
        flex: 1,
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Text style={{ fontSize: 20 }}>voice Screen</Text>
    </View>
  );
}
